# Minimum versions (fresh-install-safe with the modern TemplateResponse signature).
# TODO(hardening): lock exact versions / add a lockfile once validated in a fresh venv.
fastapi>=0.115
uvicorn>=0.30
jinja2>=3.1
python-multipart>=0.0.9
python-dotenv>=1.0
truststore>=0.9
anthropic>=0.39
httpx>=0.27
pypdf>=5.0
python-docx>=1.1
pytest>=8.0
