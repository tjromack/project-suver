"""Project Suver — the product: an AI tool hub that removes the prompt.

This repo holds the reusable tool-app shell, the hub launcher, and the tools. First tool: Summarize (drop a
real document -> a cited summary, sanitized before egress, 3 clicks, no prompt). See DESIGN.md / _PLATFORM/VISION.md.
"""

__version__ = "0.1.0"
