# Real test documents (local only)

Real, non-sensitive documents for testing the live Summarize experience — **gitignored** (they're large binaries
and test inputs, not source). The committed fixture is `../sample.txt`.

- `FSOC2025AnnualReport.pdf` — a long report (regulatory annual report).
- `Byzantine_navy.pdf` — a long-form article.
